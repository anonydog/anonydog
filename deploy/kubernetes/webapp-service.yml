kind: Service
apiVersion: v1
metadata:
  name: webapp
spec:
  ports:
    # Accept traffic sent to port 80
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: webapp
  type: LoadBalancer
